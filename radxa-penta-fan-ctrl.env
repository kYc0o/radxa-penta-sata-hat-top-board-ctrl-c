# Environment configuration for Radxa Penta Fan Controller
# These variables are read by the daemon at startup and by systemd via EnvironmentFile

# Use hardware PWM via /sys/class/pwm (1) or software PWM using libgpiod (0)
HARDWARE_PWM=0

# PWM chip index when HARDWARE_PWM=1 (pwmchipN -> N)
PWMCHIP=0

# Software PWM GPIO configuration (when HARDWARE_PWM=0)
# GPIO chip index (maps to /dev/gpiochipN)
FAN_CHIP=0
# GPIO line (BCM numbering for Raspberry Pi; adjust per board)
FAN_LINE=27

# Front button GPIO (optional; enables page switching on the OLED)
BUTTON_CHIP=0
BUTTON_LINE=17

# Debug logging level:
#   0 = off (default)
#   1 = normal (startup config + fan status updates)
#   2 = verbose (includes detailed thermal/PWM debug output)
RADXA_DEBUG=0

# You can override these per-machine by creating /etc/radxa-penta-fan-ctrl/radxa-penta-fan-ctrl.env
# and restarting the service:  sudo systemctl restart radxa-penta-fan-ctrl

